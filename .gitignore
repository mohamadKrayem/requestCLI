# Build output
requestCLI
dist/

# Test and coverage artifacts
*.test
coverage.out
coverage.html

# Local environment
.env
.env.local

# Product strategy notes, kept local on purpose
VISION.md

# Locally installed agent skills, not part of the project.
# Scoped to .claude/skills/ rather than all of .claude/ so project-local
# config (CLAUDE.md, shared commands) stays trackable.
.agents/
.claude/skills/
skills-lock.json

# Editor / OS noise
.idea/
.vscode/
.DS_Store
